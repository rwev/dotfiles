# Machine-local overrides — never commit these
.gitconfig.local

# Claude Code — machine-local overrides / runtime, never commit
.claude/settings.local.json
.claude/**/*.local.json
.claude/skills/synced/

# macOS
.DS_Store
